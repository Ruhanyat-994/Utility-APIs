<!DOCTYPE html>
<html>
<head>
    <title>Palindrome Checker</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>🔄 Palindrome Checker</h1>
        <p class="subtitle">Check if your word is a palindrome in style 🚀</p>

        <form action="/" method="get" class="checker-form">
            <input type="text" name="keyword" placeholder="Enter keyword" value="{{keyword}}" required />
            <button type="submit">Check</button>
        </form>

        {{#result}}
            <p class="result {{#isPalindrome}}success{{/isPalindrome}}{{^isPalindrome}}error{{/isPalindrome}}">
                {{#isPalindrome}}🎉 Congratulations! "{{keyword}}" is a palindrome!{{/isPalindrome}}
                {{^isPalindrome}}😞 Sorry! "{{keyword}}" is NOT a palindrome.{{/isPalindrome}}
            </p>
        {{/result}}
    </div>

    <script src="/js/script.js"></script>
</body>
</html>
